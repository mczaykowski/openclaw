// Note: vitest excludes **/*.e2e.test.ts by default in this repo.
// The active test lives in openclaw-tools.temporal-smoke.test.ts.

import { describe, it } from "vitest";

describe.skip("temporal_smoke tool (e2e placeholder)", () => {
  it("moved to .test.ts", () => {});
});
